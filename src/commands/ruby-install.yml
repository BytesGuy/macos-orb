description: |
  Install the specified version of Ruby

parameters:
  version:
    description: "Ruby version required"
    type: string
steps:
  - run:
    name: "Installing Ruby version << parameters.version >>"
    command: ruby-install ruby-<< parameters.version >>
