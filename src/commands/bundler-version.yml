description: |
    Installs the specified Bundler version

parameters:
    version:
        description: "system"
        type: string
steps:
    - run:
        name: "Installing Bundler version << parameters.version >>"
        command: gem install bundler:<< parameters.version >>