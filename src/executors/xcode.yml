description: |
  A macOS based VM using the specified Xcode version

parameters:
  version:
    type: string
    default: "11.3.1"
    description: |
      For a full list of supported versions, see
      https://circleci.com/docs/2.0/testing-ios/#supported-xcode-versions

macos:
  xcode: << parameters.version >>
